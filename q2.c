#include<stdio.h>
int main()
{
  int a, b, sum, diff, prod, quo;
  printf("Enter a:");
  scanf("%d", &a);
  printf("Enter b:");
  scanf("%d", &b(;
  sum = a + b;
  diff = a - b;
  prod = a * b;
  quo = a / b;
  printf("The sum is:", sum);
  printf("The difference is:", diff);
  printf("The prod is:", prod);
  printf("The quotient is:", quo);
  return 0;
}
